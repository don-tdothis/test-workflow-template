version: 3
defaults:
  workflow: custom_workflow
  roles:
    aws: arn:aws:iam::624472603577:role/test-terrahaxs
projects:
  - dir: tf
    apply_requirements: [mergeable, approved]
workflows:
  custom_workflow:
    plan:
      steps:
        - init
        - plan
    apply:
      steps:
        - init
        - apply
