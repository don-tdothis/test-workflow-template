version: 3
automerge: true
defaults:
  workflow: custom_workflow
projects:
  - dir: tf
workflows:
  custom_workflow:
    plan:
      steps:
        - env:
            name: FOO
            value: BAR
        - init
        - plan
