version: 3
automerge: true
defaults:
  workflow: custom_workflow
projects:
  - dir: dir1
workflows:
  custom_workflow:
    plan:
      steps:
        - env:
            name: FOO
            value: BAR
        - init
        - plan
